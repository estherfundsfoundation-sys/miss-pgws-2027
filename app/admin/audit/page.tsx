import{AdminFrame}from"../AdminFrame";export default function Page(){return <AdminFrame current="/admin/audit" title="Security & audit log." intro="Review role changes, application decisions, score corrections, agreement versions, account recovery, and sensitive administrative actions."><div className="panel"><h2>Protected event history</h2><p>Audit events are append-only and never store passwords, access tokens, reset links, payment credentials, or other secrets.</p></div></AdminFrame>}
