import Link from "next/link";import{DashboardHeader}from"../components/DashboardHeader";
const links=[['Overview','/admin'],['Applicants','/admin/applications'],['Review queue','/admin/reviews'],['Contestants','/admin/contestants'],['Scoring','/admin/scoring'],['Voting & finance','/admin/finance'],['Configuration','/admin/settings'],['Audit log','/admin/audit']];
export function AdminFrame({current,title,eyebrow='COMPETITION OPERATIONS',intro,children}:{current:string;title:string;eyebrow?:string;intro:string;children:React.ReactNode}){return <main className="dashboard-shell"><DashboardHeader staff/><div className="dashboard-grid"><aside className="dashboard-nav"><p className="eyebrow eyebrow--light">AUTHORIZED STAFF</p><nav>{links.map(([label,href])=><Link aria-current={current===href?'page':undefined} href={href} key={href}>{label}</Link>)}</nav></aside><section className="dashboard-main"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="lede">{intro}</p>{children}</section></div></main>}
