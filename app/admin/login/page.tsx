import type { Metadata } from "next";import { AuthShell } from "../../components/AuthShell";export const metadata:Metadata={title:'Staff Sign In'};export default function Page(){return <AuthShell mode="staff"/>;}
